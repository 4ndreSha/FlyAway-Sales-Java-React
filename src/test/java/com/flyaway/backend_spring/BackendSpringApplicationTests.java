package com.flyaway.backend_spring;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class BackendSpringApplicationTests {

	@Test
	void contextLoads() {
		/*
		 * This test verifies that the Spring application context loads successfully.
		 * The test passes if the application context starts without errors.
		 * No additional assertions are needed as the mere absence of exceptions
		 * during context loading constitutes a successful test.
		 */
	}
}
